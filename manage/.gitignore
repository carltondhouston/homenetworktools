# Secrets — never commit these
.env

# Audit CSV output (ephemeral)
audit_output/*.csv

# Baseline IS committed — it's the source of truth for change detection
# audit_output/npm_baseline.json  ← intentionally not ignored

# Python
__pycache__/
*.pyc
.venv/
